name: Deploy to Server

on:
  push:
    branches:
      - dev  # Déclenche le workflow sur les push vers la branche principale

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Setup SSH
      uses: webfactory/ssh-agent@v0.5.3
      with:
        ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}
      env:
        GIT_SSH_COMMAND: "ssh -o LogLevel=ERROR"
        
    

    - name: Copy files via SCP
      run: |
        scp -q ./a.txt ${{ secrets.SECRET_USER }}@${{ secrets.SECRET_SERVER }}:/app/.

    #- name: Execute remote command via SSH
      #run: |
       # ssh user@your-server 'cd /path/on/server && ./deploy-script.sh'
